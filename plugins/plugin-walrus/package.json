{
  "name": "@sui-walrus/plugin-walrus",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "ElizaOS plugin: store and read erasure-coded blobs on Walrus.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "tsx --test test/**/*.test.ts"
  },
  "//tests": "Unit tests were moved to ../../proofs/plugins/plugin-walrus (archived, source-only plugins/). To run them, move the *.ts files back into test/ here; the test script above expects test/.",
  "dependencies": {
    "@elizaos/core": "^1.7.2",
    "@mysten/sui": "^2.17.0",
    "@mysten/walrus": "^1.1.7"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
