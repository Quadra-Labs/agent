{
  "name": "@sui-walrus/agent",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "ElizaOS agent app: boots the runtime with all four plugins (sql, groq, walrus, memwal) live.",
  "scripts": {
    "chat": "tsx src/cli.ts",
    "typecheck": "tsc --noEmit"
  },
  "//proofs": "Proof/smoke scripts were moved to ../../proofs/apps (archived, source-only apps/). To run them, move the files back into src/ and restore the boot/roundtrip/*:proof/test scripts (see git history or ../../proofs/apps).",
  "dependencies": {
    "@elizaos/core": "^1.7.2",
    "@elizaos/plugin-groq": "^1.0.4",
    "@elizaos/plugin-sql": "^1.7.2",
    "@mysten/bcs": "2.0.5",
    "@mysten/seal": "1.1.3",
    "@mysten/sui": "2.17.0",
    "@mysten/walrus": "1.1.7",
    "@sui-walrus/plugin-walrus": "file:../plugins/plugin-walrus",
    "@sui-walrus/plugin-memwal": "file:../plugins/plugin-memwal"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
